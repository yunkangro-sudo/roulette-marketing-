# DESIGN.md — 디자인 시스템 원칙

이 문서는 실제 화면(소비자 게임 플로우, 광고주 관리자)을 만들 때 참고하는 디자인 기준이다. 톤·색·폰트를 화면마다 즉흥적으로 정하지 않기 위한 최소 기준선이며, 완성된 디자인 시안이 아니라 **일관성을 지키기 위한 규칙**이다.

---

## 레퍼런스

**토스** — 색과 문구를 다 쓰지 않고, 필요한 한 곳에만 강하게 몰아주는 절제. 이 제품은 사장님(신뢰가 필요한 쿠폰/금전 관리)과 손님(가볍고 즉각적인 게임 경험) 양쪽을 만족시켜야 한다. 관리자 화면은 토스 톤(절제·신뢰)을 따르고, 소비자 게임 화면은 그 절제 범위 안에서 순간적인 생동감을 준다.

---

## 컬러 토큰

**적용 범위 구분**: 아래 두 세트를 구분해서 쓴다. `제품(관리자·소비자 게임 화면)`은 기존 원칙(포인트 컬러 1개)을 유지하고, `랜딩페이지`는 카카오·당근 듀오톤을 의도적으로 추가한다. 제품 UI에 듀오톤을 끌어오지 않는다 — 랜딩은 "우리는 두 채널을 다 잘 다룬다"는 메시지를 전달하는 자리이고, 제품 화면은 절제된 신뢰를 전달하는 자리라 목적이 다르다.

### 제품 UI 토큰 (기존 유지)

| 토큰 | HEX | 용도 |
|---|---|---|
| `--ink` | `#16181D` | 본문 텍스트 |
| `--paper` | `#FFFFFF` | 기본 배경 |
| `--surface` | `#F4F6F8` | 카드/섹션 배경 |
| `--border` | `#E4E8ED` | 구분선, 입력필드 테두리 |
| `--brand` | `#3D5AFE` | **유일한 포인트 컬러.** CTA 버튼, 강조 텍스트, 대시보드 핵심 지표 전용 |
| `--success` | `#1FC77A` | 전환 성공/완료 표시 전용 (데이터 시각화에만 한정, CTA 버튼에는 쓰지 않음) |

**규칙**: `--brand`는 반드시 하나로 제한한다. "여기를 누르면 된다"는 신호가 화면마다 헷갈리지 않게, CTA는 항상 같은 색으로 보이게 한다. 초록(`--success`)은 이미 완료된 것을 나타낼 때만 쓰고, 눌러야 할 버튼에는 절대 쓰지 않는다.

### 랜딩페이지 전용 토큰 (듀오톤 액센트)

| 토큰 | HEX | 용도 |
|---|---|---|
| `--landing-ink` | `#14151A` | 프리미엄 느낌의 짙은 잉크 (제품보다 살짝 더 어둡게, 권위감) |
| `--landing-paper` | `#FAFAF8` | 완전한 흰색 대신 아주 옅은 웜톤 오프화이트 |
| `--kakao-yellow` | `#FEE500` | 카카오 공식 브랜드 옐로우. 카카오 채널 관련 배지·아이콘·하이라이트 전용 |
| `--danggeun-orange` | `#FF8A3D` | 당근 브랜드 오렌지 계열 (근사치 — 실제 사용 전 당근 공식 브랜드 가이드에서 정확한 헥스 재확인 필요) |
| `--merge-gradient` | `linear-gradient(135deg, #FEE500, #FF8A3D)` | 히어로 섹션 등 "두 채널이 만나는 지점"을 상징하는 단 하나의 시그니처 그라디언트 |

> **2026-08 실제 구현 노트**: landing-v5(현재 루트 `/`)는 위 표의 값 그대로가 아니라, `components/landing-v5/landing-v5.css`의 `.landing-v5` 클래스 스코프 안에 CSS 커스텀 프로퍼티로 별도 정의된 토큰을 쓴다 (Tailwind 테마가 아니라 클래스 스코프 변수라 다른 페이지에서 `Navbar` 등 landing-v5 컴포넌트를 재사용하려면 반드시 `.landing-v5` 래퍼로 감싸야 함 — `/signup`이 이 패턴을 따름). 실제 값: `--green:#00c7a7`(민트그린, 브랜드 포인트 컬러) / `--green-deep:#019c87` / `--green-tint:#e3fbf6` / `--carrot:#ff8a00`(당근) / `--kakao:#fee500` / `--ink:#222222` / `--bg:#faf7f0`(웜톤 오프화이트). 위 표의 `--landing-ink`/`--landing-paper`/`--danggeun-orange` 근사치보다 이 실측 토큰이 우선한다.

**절대 규칙 — 가장 중요**:
1. **페이지 전체를 노랑·주황으로 칠하지 않는다.** 기본 배경은 `--landing-paper`(프리미엄 오프화이트) 또는 `--landing-ink`(다크 섹션) 유지. 카카오 옐로우·당근 오렌지는 **각 채널을 설명하는 카드·배지·아이콘에만 국한**해서 쓴다.
2. **`--merge-gradient`는 페이지 전체에서 딱 한 곳(히어로)에만 쓴다.** 여러 군데 쓰면 "두 채널이 만난다"는 의미가 희석되고 유치해 보인다.
3. 카카오 콘텐츠 카드는 옐로우 배경 + 검정 텍스트(카카오 본사가 실제로 쓰는 조합), 당근 콘텐츠 카드는 오렌지 텍스트/배지 + 흰 배경(당근이 흰 배경에 로고를 쓰는 방식과 동일) — 각 브랜드가 실제로 쓰는 배색 관례를 따라야 "우리가 이 채널을 제대로 이해하고 있다"는 신뢰가 생긴다.

---

## 타이포그래피

| 역할 | 폰트 | 이유 |
|---|---|---|
| 제목/디스플레이 | **SUIT** (또는 Paperlogy — 실제 렌더링 비교 후 확정) | Pretendard 단독 사용은 이미 국룰이라 무난하지만 개성이 없음 |
| 본문 | **Pretendard** | 가독성 검증된 선택, 본문에서는 무난함이 오히려 장점 |
| 데이터/코드 (쿠폰코드, %, 지표 숫자) | **JetBrains Mono** | "482 731" 같은 쿠폰 코드나 전환율 수치에 모노스페이스를 적용하면 신뢰도 있는 데이터 느낌을 줌 |

---

## 레이아웃 원칙

- **소비자 화면(모바일 퍼스트)**: 한 화면에 하나의 행동만. 버튼은 항상 1개, 여러 선택지를 동시에 주지 않는다.
- **관리자 화면**: 정보 밀도를 낮춘다. 지표는 한 번에 4개 이하로 노출하고, 나머지는 접어서(아코디언/탭) 숨긴다.
- **터치 타겟**: 모든 탭 가능 요소는 최소 44x44px.
- **폼 필드**: 화면당 최대 2개 (전화번호 + 동의 체크박스 수준 유지).

---

## 시그니처 요소 (이 제품만의 기억점, 1개로 제한)

**"게임 결과 → 전환 액션까지 하나의 끊기지 않는 모션."** 스크래치카드가 긁히는 순간부터 보상 확인, 쿠폰 발급, CTA 버튼 등장까지를 Framer Motion으로 하나의 연속된 시퀀스로 설계한다. 이 모션 하나에만 디테일을 집중 투자하고, 나머지 화면(대시보드, 설정, 목록 화면 등)은 철저히 절제한다.

**적용 범위 주의**: 이 시그니처 모션은 소비자 게임 화면(`/play/[storeId]`)에만 적용한다. 관리자 화면에 과도한 애니메이션을 넣지 않는다 — 매일 쓰는 업무 화면에 불필요한 모션은 오히려 방해가 된다.

---

## 랜딩페이지 전용 톤 가이드 (제품 UI와 별도)

랜딩페이지는 "심플하지만 내공이 느껴지는 전문 에이전시" 인상이 목표다. 이를 위해 제품 UI보다 타이포그래피 크기 대비를 과감하게 준다 — 헤드라인은 아주 크게, 본문은 충분히 작게, 그 사이 여백을 넉넉하게 둬서 "여유 있는 브랜드"라는 인상을 준다. 정보를 빽빽하게 채우지 않는다. 데이터/수치(시장 성장률 등)는 반드시 JetBrains Mono로 표기해서 "근거 있는 주장"이라는 느낌을 강화한다.

## AI 기본값 회피 체크 (화면 완성 직전 필수 확인)

- [ ] 크림베이지+세리프+테라코타 조합이 아닌가
- [ ] 다크+네온 단색 조합이 아닌가
- [ ] 신문 칼럼형 레이아웃이 아닌가
- [ ] 포인트 컬러(`--brand`)가 정말 하나로만 쓰였는가 (CTA와 강조 텍스트에 다른 색이 섞이지 않았는가)
- [ ] 시그니처 모션이 소비자 게임 화면에만 집중되고, 관리자 화면은 절제됐는가

---

## 컴포넌트별 적용 메모

| 컴포넌트 | 적용 규칙 |
|---|---|
| Button (primary) | `--brand` 배경, 화면당 강조 버튼은 1개만 |
| Button (secondary) | `--border` 테두리 + `--ink` 텍스트, 배경 없음 |
| Card | `--surface` 배경, `--border` 1px, radius 12px |
| 쿠폰 코드 표시 | JetBrains Mono, `--ink` 굵게 |
| 전환 성공 상태(뱃지 등) | `--success` 배경/텍스트, CTA와 혼동되지 않게 버튼 형태로 만들지 않음 |
| 에러 메시지 | 별도 error 토큰 필요 시 `#E5484D` 추가 검토 (현재 토큰 세트에는 미포함 — 실제 필요 시점에 추가) |

---

## 미확정 — 코딩 중 확정 필요

- SUIT vs Paperlogy 최종 선택: 쿠폰 코드/퍼센트 숫자와 함께 실제 렌더링해보고 결정
- 에러 상태 컬러 토큰 (현재 6개 토큰에는 미포함, 폼 검증 화면 만들 때 추가)
- 다크모드: 이번 스코프 제외 (README 참고)
