# 단골팅 관리자 모드 — 결정사항 & 진행상황

> 이 문서는 관리자 모드(광고주/슈퍼관리자) 개발 중 Claude와 논의해서 확정한 결정사항을 추적하기 위한 문서입니다.
> 새 결정이 나올 때마다 이 파일을 업데이트하세요. 커서에게 컨텍스트를 줄 때도 이 파일을 같이 보여주면 됩니다.
> 마지막 업데이트: 2026-09-01

---

## ✅ 완료된 기능

### 리워드 카탈로그
- 본인확인(당근 단골) 토글: 폼에서 켜고 끄기 가능, 끄면 계산대 확인 단계 없이 즉시 지급
- 할인쿠폰 선택 시 할인금액 필드 조건부 노출 (유형→필드 매핑 구조로 확장 가능하게 구현)
- 리워드 수정 모달 (기존값 채워짐 + "지금 손님 화면에 보이고 있어요" 노출상태 배지)
- 손님용 리워드샵: 이미지 카드형, 포인트 부족 시 진행률 막대바, 본인확인 배지
- 쿠폰함에 보유쿠폰 + 리워드 교환내역 통합 노출
- 테스트 더미데이터 정리 스크립트 실행 완료
- **리워드 이미지 파일 업로드** — URL 입력 방식에서 Storage 버킷 기반 실제 파일 업로드로 전환 완료 (`040_reward_images_storage_bucket.sql`)
- **리워드 교환을 쿠폰함(coupons)에 통합** — 별도 `rewards_issued` 테이블 대신 게임 당첨 쿠폰과 동일한 `coupons` 테이블·화면·계산대 흐름을 재사용하도록 통합 (`041_reward_redemption_coupons_integration.sql`)
- **리워드 교환 최소잔액(threshold) 게이트 완전 제거** (`042_remove_usage_threshold_gate.sql`)

### 포인트 적립/차감 시점
- **포인트/재고 차감 시점 이동 완료** — "교환하기" 클릭 시점(선점)에서 "사장님 확인"(실사용 확정) 시점으로 이동. `confirm_coupon_used_atomic` RPC로 원자 처리 (`043_defer_point_deduction_to_confirm.sql`)

### 슈퍼관리자 모드
- **전체 대시보드 / 업체 리스트 / 업체 상세(이용기간·결제 탭)** — 대리접속(impersonation) 방식으로 구현 완료, 감사로그(`impersonation_log`) 적용
- **업체 완전삭제(Hard Delete)** — `delete_store_completely` RPC (SECURITY DEFINER)
- **"월 광고비" → "월 구독료" 용어 통일** 완료 (코드/화면 전체)
- **대시보드 KPI 6종 확장** — 업체 가입추이, 전체 회원수, 당근 클릭수, 리워드 유형별 비율, 쿠폰 사용률, 평균 재방문율
- **"업체 구독관리" 신규 메뉴** — 결제일/결제상태(`payment_date`/`payment_status`) 추가, 검색·필터, 입금확인 처리, CSV 다운로드 (`049_subscription_payment_fields.sql`)

### 성과 리포트
- **월광고비/ROI 퍼널 방식 → 객단가 기반 스토리텔링형 리포트로 전면 개편** — 헤드라인(구독료 대비 배수)/활동퍼널(MoM)/단골전환스토리/당근단골자산가치/성장궤적(3개월 예측)/인기 시간대 6개 섹션. "평균 결제금액(객단가)" 입력 필드를 포인트 정책 화면에 추가

### 랜딩페이지 / 도메인
- **landing-v5 전면 리뉴얼 및 루트(`/`) 승격** — 구 랜딩(`/landing`) 삭제, 헤더를 햄버거+슬라이드 패널로 재구성
- **Vercel 기본 도메인 → `www.dgting.co.kr` 영구 리다이렉트** — Vercel URL이 어떤 화면에도 노출되지 않도록 처리 완료

### 방문 적립 (NFC)
- **NFC 태그 기반 방문 적립 신규 구현 완료** — 아래 "🔒 보류 결정" 항목 참고 (방향이 뒤집혀서 실제 구현됨)

### QR 코드 생성/다운로드
- **매장 고정 QR코드 자동 생성 완료** — `GET /api/admin/store-qr`가 `qrcode` 라이브러리로 매장별 QR을 즉석 생성(DB 저장 없음, `storeId`만으로 매번 결정적으로 생성). "이벤트 관리" 화면 상단에 QR 미리보기 + PNG(960px)/SVG 다운로드 버튼 + 복사 가능한 URL 텍스트 노출. 에러정정 최고단계(H) 적용. QR이 가리키는 `/play/{storeId}`는 항상 "현재 활성 이벤트"를 조회하므로 이벤트를 몇 번 바꿔도 QR 자체는 절대 안 바뀜

### 매장 공개 홈페이지 (Business Page)
- **`/b/{storeId}` 신규 구현 완료** (`051_business_page.sql`) — 모든 매장에 기본 공개(무료/유료 구분 없이 자동 생성), `homepage_enabled` 기본 true·`online_play_enabled` 기본 false. 히어로/신뢰지표(10명 미만 자동숨김)/이벤트미리보기/리뷰링크/매장정보/FAQ 섹션 + `LocalBusiness` JSON-LD. 관리자 신규 메뉴 `/admin/business-page`에서 전부 입력·관리. 쿠폰함 상단에 바로가기 노출
- **SEO 기반(sitemap/robots) 신규 구현 완료** — `/b/*`만 검색엔진에 노출, 나머지 개인화 영역 차단
- **확장(업종별 라벨/대표상품/오늘·이번달 토글/매장 자랑) 구현 완료** (`052_business_page_v2.sql`) — 업종(`business_type`)에 따라 "⑤ 대표 상품/메뉴/서비스" 섹션 이름이 자동으로 바뀌는 라벨 매핑(`lib/business-page/businessTypeLabels.ts`), 대표 상품/메뉴/서비스 최대 6개, 참여자 수·재방문율·지급된 혜택 수를 오늘/이번달 토글로 보여주는 신뢰지표 확장, 주차정보/반려동물 동반 표시, "우리 매장의 자랑" 최대 4개. "당근 소식 자동발행" 재사용 전제는 실제로 그런 기능이 없어서 이번 범위에서 제외(`docs/v2_로드맵/매장홈페이지_당근소식섹션.md` 참고), 온라인 게임 참여 기본값/소유권은 기존 결정(기본 OFF·매장관리자 직접 토글) 그대로 유지
- **히어로 커스터마이징 (`tagline`/`game_cta_label`) 구현 완료** (`054_business_entity_hero_copy.sql`) — 히어로 섹션에 감성 헤드라인(2줄, 줄바꿈 유지)과 게임 버튼 문구를 매장별로 다르게 설정 가능. 둘 다 비어있으면 기존과 동일(하위 호환)

### 영업 시연용 샘플(데모) 매장
- **데이터 격리 인프라 완료** (`053_demo_store_isolation.sql`) — `store_contracts.is_demo`(기본 false). true인 매장은 슈퍼관리자 대시보드/구독관리 집계, sitemap에서 자동 제외되고 `/b/{slug}`에 `noindex` 자동 삽입, 알림톡·카카오 발송 함수에도 이중 방어코드 적용. 업체 리스트에 "샘플" 필터 탭 추가(기본 "전체" 탭 등에서는 항상 숨김)
- **샘플 매장 10곳 콘텐츠 생성 완료** (`scripts/seed-demo-stores.mjs`) — 음식점/카페/미용실/네일샵/헬스장/에스테틱/베이커리/세차/키즈카페/펫살롱. 매장마다 로그인계정(`store_accounts`)·1년 이용기간(`subscriptions`)·포인트정책(1회=100p, `loyalty_settings`)·즉시쿠폰 3개+꽝(percent 모드·수량 무제한이라 재고 소진 걱정 없음)·장기 리워드 2개·대표상품 4개·매장홈페이지 콘텐츠까지 자동 생성. 재실행하면 해당 매장 데이터를 지우고 다시 만듦(멱등)
- **⏳ 미완료**: Phase 2(8~12주 → 사용자 요청으로 3~8주로 축소) 가상 활동데이터(가짜 손님/방문/쿠폰발급/세그먼트) 생성 스크립트, 슈퍼관리자 "샘플 레퍼런스" 메뉴(재생성 버튼) — `upsert_customer_loyalty` RPC가 `now()` 고정이라 과거 날짜 시뮬레이션에 못 써서 별도 스크립트로 직접 테이블에 기록하는 방식으로 설계 필요

## ⏳ 방향은 확정, 실제 작업/적용 여부 미확인

- **계산대 보안 방식**: PIN 방식 → 기기 페어링 → 최종적으로 "NFC 태그(포인트 적립) + 고객 QR을 직원이 스캔(쿠폰/리워드 사용) + 동적 QR(30초 갱신, 캡처 재사용 방지)" 조합으로 결정. NFC 방문적립 파트는 구현 완료(아래 참고)했지만, "고객 QR 스캔 + 동적 QR" 파트는 아직 미구현 — 계산대(`/staff`)는 현재도 쿠폰 코드 직접 입력/조회 방식 그대로임
- **Supabase Pro 업그레이드**: 진행 여부 최종 확인 필요. 완료 후 자동백업(PITR) 활성화 확인 필요

## 🆕 미시작 — 방향만 정해짐

- **캐릭터 프리셋**: 완전커스텀/색상만 방식 기각, 프리셋 라이브러리(3~5종)+기존 5색 조합 채택. 종류/개수 미정.
- **포인트 시각화 스킨**: 도장판(②안) 채택, NFC 스탬프 모드에서 `StampBoard` 컴포넌트로 1차 구현됨 → 게이지바 스타일 등 추가 디자인 다듬기는 나중 작업
- **이벤트별 개별 QR**: 수요 확인되면 진행 (events.slug 필드만 미리 추가해둠, 로직은 미구현)
- **AEO 홈페이지 제작 상품**: 랜딩 요금제에 카드는 노출됨, `/aeo`는 자리표시 페이지 + 대기자 등록(`aeo_waitlist`)만 있고 실제 상품/제작 프로세스는 미착수

## 🔒 보류 결정 → 재검토됨

- **방문 스탬프 별도 시스템**:~~안 만들기로 결정~~ → **2026-09-01 재검토, NFC 방문 적립 기능으로 실제 구현함.** 매장에 놓은 NFC 태그를 손님이 태그하면 포인트 또는 스탬프(목표 도달 시 리워드 쿠폰 자동발급) 중 매장이 선택한 모드로 적립되는 별도 경로 (`050_nfc_checkin.sql`, `process_nfc_checkin` RPC). 게임/리워드샵 로직과는 완전히 분리되어 있어 기존 흐름에 영향 없음. 실제 매장 현장에서 NFC 태그로 테스트하는 것은 아직 안 함(README "배포 전 반드시 확인할 것" 참고)
- **당근마켓 제휴(단골팅 자체 노출)**: 특허 출원 완료 전까지 보류. 당근 광고대행사 인증은 별개 트랙으로 진행 가능 (단골팅 얘기 노출 안 함)

## 📌 고정 설계 원칙 (변경 시 신중히)

- 게임 실행은 채널(카카오/당근) 구독 여부와 완전히 무관해야 함 (특허 회피)
- 매장당 동시 활성 이벤트는 1개 (도전횟수 설정: 매일/주간/월간/무제한, 롤링 기준)
- 이용기간 만료 시 유예기간 7일 후 차단, /staff는 예외, 슈퍼관리자 대리접속 중엔 미적용
- 광고 중단 모드(자발 토글)는 이용기간 만료 차단과 완전 별개
- 개인정보(전화번호 등)는 마스킹 표시만, 대량 엑셀 다운로드 기능 없음
- 커서 지시문은 사용자가 명시적으로 요청할 때만 작성 (논의 단계에선 대화로만)

## 🗂 다른 채팅방에서 진행 중인 것

- 특허 조사 (인형뽑기 게임 방식 + 당근 단골추가 연계 마케팅 구조) — 별도 채팅방에서 KIPRIS 조사 진행 중

---

## 다음 작업 추천 순서

1. Supabase Pro 업그레이드 완료 확인 (돈 걸린 데이터라 최우선, 유일하게 계속 미확인 상태)
2. NFC 방문적립 실제 현장 태그 테스트 (하루1회 제한/포인트·스탬프 모드/계산대 스캔)
3. 계산대 "고객 QR 스캔 + 동적 QR" 파트 — NFC 포인트 적립은 됐으니 나머지 절반만 남음
4. 알림톡(비즈메시지) 발송대행사 실연동
5. 매장 고정 QR코드 실제 인쇄해서 스캔 테스트 (에러정정 H단계가 실제 코팅지 환경에서도 잘 읽히는지)
6. 캐릭터 프리셋 (종류 확정 후)
7. AEO 홈페이지 제작 상품 실제 설계
