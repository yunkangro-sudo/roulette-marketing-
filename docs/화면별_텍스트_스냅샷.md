# 화면별 텍스트 스냅샷

작성일: 2026-08-14  
목적: QR 스캔부터 계산대 처리까지, 실제 화면에 렌더링되는 문구·버튼·요소를 위에서 아래로 그대로 읽으며 검토하기 위함.  
규칙: 코드에 적힌 문자열을 요약·각색하지 않는다. `{변수}`는 런타임 값.

컴포넌트 기준: `PlayFlow.tsx` 손님 여정 v2 (게임 먼저 → 결과 잠금 → 로그인으로 해제).

---

## 화면 1. QR 스캔 직후 랜딩 (경로: `/play/[storeId]`)

소스: `app/(customer)/play/[storeId]/PlayFlow.tsx` (`step === 'landing'`)

### 활성 이벤트가 있을 때

- 상단: `[내 쿠폰보관]` → `/me/points?store_id={storeId}`
- 본문:
  - 🥕 (큰 아이콘, 썸네일 자리)
  - `이벤트 썸네일`
  - `{event.name}` (해당 매장 활성 이벤트명. 예: DB에 저장된 이름 그대로)
  - `로그인 없이 바로 도전해 보세요!`
- 버튼: `[뽑기 시작]` → `step = 'playing'` (같은 URL, 화면 2)
- 하단: `1일 1회 응모 가능`

다음 화면으로: `[뽑기 시작]`을 누르면 화면 2.

### 활성 이벤트가 없을 때 (같은 경로)

소스: `PlayFlow.tsx` (`!event`)

- 본문:
  - 🥕
  - `현재 진행중인 이벤트가 없어요`
  - `매장에서 새로운 이벤트를 준비 중이에요.`
  - `조금만 기다려 주세요!`
- 하단: `store: {storeId}`
- 버튼: 없음 (여기서 종료)

### 로딩 / 결과 확정 중 (같은 경로, 잠깐 보임)

- `로딩 중...` (`step === 'loading'`)
- `결과를 확인하고 있어요...` (`step === 'claiming'`)

---

## 화면 2. 게임 플레이 (경로: `/play/[storeId]`, `step === 'playing'`)

소스: `GameContainer` (`initialPhase="play"`) + `PlayScreen.tsx` + 최초 1회 `OnboardingOverlay.tsx`

랜딩에서 이미 `[뽑기 시작]`을 눌렀으므로 `StartScreen`(「당근 인형뽑기 / 지금 도전하고 쿠폰 받아가세요!」)은 **이 여정에서 보이지 않는다.**

### 2-A. 최초 1회 온보딩 오버레이

소스: `components/game/claw_machine/OnboardingOverlay.tsx`  
조건: `sessionStorage` 키 `game_onboarding_seen`이 없을 때. 3초 후 자동 닫힘.

- 본문:
  - `손가락으로`
  - `움직이다 놓으면 뽑혀요!`
  - ◀ ▶ 와 👆 애니메이션
  - `손을 떼는 순간 자동으로 뽑혀요`
- 버튼: `[스타트]` → 오버레이 닫힘, 게임 화면만 남음
- 하단: (버튼이 하단)

다음 화면으로: 스타트 또는 3초 후 → 2-B.

### 2-B. 드래그 인터랙션 화면

소스: `components/game/claw_machine/PlayScreen.tsx`

- 상단: `← 드래그해서 위치 조정 후 손 떼기 →`
- 본문:
  - 가로 레일 + 노란 크레인 (좌우 드래그)
  - 집게
  - 진열장 3×3, 캐릭터 9칸 모두 🥕
- 버튼 (5초간 드래그/탭이 없을 때만 중앙에 나타남):
  - `[여기를 눌러도 뽑을 수 있어요 👆]` → 크레인 하강과 동일 (추첨 실행)
- 하단: 진열장
- 오류 시 브라우저 alert: `오류가 발생했습니다. 다시 시도해주세요.`

손을 떼거나 폴백 버튼을 누르면 `POST /api/games/play` 후 크레인 연출. 당첨액은 화면에 나오지 않음.

다음 화면으로: 연출 끝 → 화면 3 (결과 잠금). 이미 로그인된 세션이면 잠금 화면을 건너뛰고 claim 후 화면 5 또는 6.

---

## 화면 3. 결과 잠금 안내 (경로: `/play/[storeId]`, `step === 'result_locked'`)

소스: `components/play/ResultLockedScreen.tsx`  
실서비스(`NEXT_PUBLIC_KAKAO_JS_KEY` 있음) 기준.

- 상단: (없음)
- 본문:
  - 🔒
  - `결과를 확인하려면 로그인하세요`
  - `게임은 이미 끝났어요.`
  - `카카오 로그인하면 당첨 결과를 보여드릴게요.`
- 버튼: `[카카오로 결과 확인하기]` → `/api/auth/kakao?storeId={storeId}&next=claim` (화면 4)
- 하단: `전화번호 동의는 알림 수신용이며, 동의하지 않아도 결과는 확인할 수 있어요`

### 카카오 키 없을 때 (개발용, 같은 컴포넌트)

- 본문 제목/설명은 위와 동일
- `개발용 Mock 로그인`
- 버튼:
  - `[test-user-1로 결과 확인]`
  - `[test-user-2로 결과 확인]`
  - `[test-user-3로 결과 확인]`
  → `POST /api/dev/mock-customer-session` 후 claim

다음 화면으로: 로그인 성공 → 화면 5 또는 화면 6.

---

## 화면 4. 카카오 로그인 (경로: 카카오 OAuth, 우리 앱 밖)

앱 안의 로그인 CTA 문구는 **화면 3과 같다.** `[카카오로 결과 확인하기]`를 누르면 카카오 인증 서버로 나간다.

- 카카오 동의 화면의 문구(카카오계정 로그인, 동의 항목 등)는 **카카오가 렌더링**하므로 우리 코드에 없다.
- 우리 쪽이 요청하는 scope: `profile_nickname`, `phone_number`, `talk_message` (`lib/auth/kakao.ts` / `app/api/auth/kakao/route.ts`). `friends`는 심사 반려로 요청하지 않음.
- 콜백: `/api/auth/kakao/callback` → `/play/{storeId}?claim=1` 로 돌아옴 (`next=claim`). 일반 링크 `/play/{storeId}` 는 항상 첫 화면(랜딩).

### `LoginScreen.tsx` 참고 (현재 PlayFlow에서 미사용)

소스: `components/play/LoginScreen.tsx`  
카카오 키 있을 때 파일에 있는 문구:

- 🥕
- `카카오로 결과 확인하기` (제목)
- `게임은 이미 끝났어요. 로그인하면 당첨 결과를 보여드릴게요.`
- 버튼: `[카카오로 결과 확인하기]` → `/api/auth/kakao?storeId={storeId}&next=claim`
- `전화번호 동의는 알림 수신용이며, 동의하지 않아도 결과는 확인할 수 있어요`

카카오 키 없을 때:

- 👤
- `결과를 확인하려면 로그인하세요`
- `카카오 로그인하면 당첨 결과를 보여드릴게요`
- 버튼: `[테스트 계정으로 로그인]` / 로딩 시 `[확인 중...]`
- `🔧 개발/테스트용 — 카카오 앱키 설정 전까지만 표시됨`
- 라벨: `테스트 계정 ID`
- input placeholder: `예: test-user-1, test-user-2`
- 칩: `test-user-1` `test-user-2` `test-user-3`

실제 손님 여정에서 보이는 로그인 UI는 화면 3이다.

다음 화면으로: 콜백 성공 후 claim → 화면 5 또는 6.

---

## 화면 5. 이미 오늘 참여 (경로: `/play/[storeId]`, `step === 'already_participated'`)

소스: `components/play/AlreadyParticipatedScreen.tsx`  
조건: `POST /api/games/claim`이 `{ alreadyParticipated: true }`. **결과는 공개하지 않음.**

- 상단: (없음)
- 본문:
  - 🌙
  - `이미 참여하셨어요!`
  - `다음 도전은 {n}월 {n}일부터 가능해요.` (또는 `매일 1번 참여할 수 있어요.` / `내일 자정이 지나면 다시 도전해주세요!`)
- 버튼: 없음 (테스트용 `[다른 계정으로 테스트]` 버튼은 실제 손님 화면에서 오해를 살 수 있어 2026-08-24 제거)
- 하단: (없음)

다음 화면으로: 없음(종료). 재도전은 다음 날 자동으로 랜딩부터 다시 시작.

---

## 화면 6. 결과 공개 (경로: `/play/[storeId]`, `step === 'result'`)

소스: `components/game/ResultScreen.tsx`  
`PlayFlow`가 `continueLabel="다음"` 과 `onContinue`를 넘긴다.

공통 상단:

- `짠! 🎰`

공통 하단 버튼 (이 여정):

- `[다음]` → 화면 8 (채널 안내). 대박/소액/꽝 동일. `[처음부터 다시 보기]` 없음.

`{result.label}` 은 `prize_tiers.label` (DB). 데모용 기본값 예: `꽝!` / `1,000원 쿠폰` / `2,000원 쿠폰` / `10,000원 쿠폰` (`gameUtils.ts`).

### 6-A. 당첨 (small 또는 big)

- 이모지: 🎉 (small) 또는 🎊 (big)
- `{result.label}`
- (포인트가 있을 때만, 화면 7과 동일 위치) `+{pointsAwarded}P 적립`
- 쿠폰이 있으면:
  - `직원에게 보여주세요`
  - `{shortCode}` (6자리 코드, 없으면 쿠폰 id 앞 6자)
  - `사용기간 ~{YYYY.MM.DD}`
  - `당근마켓 단골 확인 후 매장에서 사용 가능`
  - `매장에서 직원에게 화면을 보여주세요`

### 6-B. 꽝 (`tier === 'miss'`)

- 이모지: 💨
- `{result.label}` (예: `꽝!`)
- `다음엔 꼭 당첨되실 거예요!`
- (포인트가 있을 때만) `+{pointsAwarded}P 적립`
- 쿠폰 블록 없음

다음 화면으로: `[다음]` → 화면 8.

---

## 화면 7. 포인트 적립 안내 (별도 경로 없음)

**독립 화면이 없다.** 매장이 `store_settings.points_enabled` 를 켰고, 방문당 포인트가 0보다 클 때만 화면 6 결과 카드 안에 한 줄이 추가된다.

소스: `components/game/ResultScreen.tsx`

- `+{result.pointsAwarded.toLocaleString()}P 적립`

스위치가 꺼져 있으면 이 줄 자체가 렌더링되지 않는다 (`pointsAwarded` 가 0). 쿠폰·결과 문구는 그대로다.

다음 화면으로: 화면 6과 동일 (`[다음]` → 화면 8).

---

## 화면 8. 카카오 채널 추가 안내 (경로: `/play/[storeId]`, `step === 'channel_cta'`)

소스: `components/play/ChannelCtaScreen.tsx`  
채널 가입 조회/기록 없음. `[카카오 채널 추가하기]`와 `[건너뛰기]` 모두 같은 다음 단계로 간다.

- 상단: (없음)
- 본문:
  - 💬
  - `매장 소식을 받아보시겠어요?`
  - `카카오 채널 추가는 선택이에요.`
  - `건너뛰어도 쿠폰과 결과는 그대로 유지됩니다.`
- 버튼:
  - URL이 있으면 `[카카오 채널 추가하기]` → 새 탭으로 `{kakao_channel_url}` 연 뒤, 건너뛰기와 **같은** 다음 단계
  - `[건너뛰기]`
  - 당첨(`amount > 0`) → 화면 10
  - 꽝 → 화면 1 랜딩
  - URL이 없으면 추가하기 버튼 숨김, 건너뛰기만
- 하단: (버튼)
- 채널 가입 조회/기록 없음 (patent-safety)

다음 화면으로: 위 분기.

---

## 화면 9. 내 포인트 (경로: `/me/points?store_id={storeId}`)

소스: `app/me/points/page.tsx`  
명칭: **내 쿠폰보관**. 세션의 `kakao_user_id`만 사용한다. URL `uid`는 무시한다.

### 파라미터 없음

- `잘못된 접근입니다. 카카오 채널 링크를 통해 접근해주세요.`

### 미로그인

- `쿠폰과 포인트를 보려면 로그인이 필요해요`
- 버튼: `[카카오로 시작하기]` → `/api/auth/kakao?storeId={storeId}&next=points`

### 로딩

- `불러오는 중...`

### 정상

- 상단 카드:
  - `내 쿠폰보관`
  - `{balance}P`
  - `보유 쿠폰 {n}장`
  - `방문 {visitCount}회 · 1회당 +{pointPerVisit}P`
  - 교환 가능: `✅ 리워드 교환 가능 ({balance}P ≥ {threshold}P)`
  - 부족: `🔒 {threshold}P 이상부터 교환 가능 ({threshold - balance}P 더 필요)`
- 성공 메시지 예: `🎉 "{reward.name}" 교환 완료! 직원에게 화면을 보여주세요.`
- 실패 메시지 예: `{data.error}` 또는 `교환 실패` 또는 `네트워크 오류가 발생했습니다`

보유 쿠폰:

- `보유 쿠폰`
- 비어 있으면: `아직 받은 쿠폰이 없어요`
- 카드: `{amount}원` / `{storeName}` / `사용기한 ~{날짜}` / `{shortCode}` / `사용가능`·`사용완료`·`만료`
- 사용가능일 때: `[계산대에서 사용]` → `/checkout/{storeId}`

미션이 있을 때:

- `🎯 진행 중인 미션`
- `{mission.name}`
- `달성 시 {n}P 적립` 또는 `달성 시 {n}원 쿠폰 지급`
- `{currentValue}/{targetValue}회`
- `앞으로 {remaining}회 더 방문하면 달성!` 또는 `달성 완료 처리 중...`
- `· 마감 {날짜}` (endAt 있을 때)

리워드:

- `리워드 교환`
- 비어 있으면: `현재 교환 가능한 리워드가 없습니다`
- 카드: `{reward.name}` / `{point_cost}P` / `품절` 또는 `잔여 {stock}개`
- 버튼: `[교환하기]` / `[처리 중...]`

내역이 있을 때:

- `포인트 내역`
- `게임 참여 적립` 또는 `리워드 교환`
- `+{amount}P` 또는 `-{amount}P`

다음 화면으로: 교환 완료 후 같은 페이지 갱신. 계산대 사용은 `/staff` 또는 `/checkout/[storeId]`.

---

## 화면 10. 당근 단골 추가 유도 (경로: `/play/[storeId]`, `step === 'verification_cta'`)

소스: `components/game/VerificationCtaScreen.tsx`  
채널 안내를 건너뛴 뒤, **당첨(amount > 0)일 때만** 온다. 금액 기준 분기는 없다(전 경품).

- 상단: `[✕]` → `/me/points?store_id={storeId}` (로그아웃하지 않음)
- 본문 아이콘: 🥕
- 본문:
  - `당근마켓 단골 추가시 쿠폰 사용가능` (한 줄, text-base)
  (2026-08-24: 하단 보조문구 `모든 경품은 당근 단골 확인 후에 매장에서 사용하실 수 있어요` 삭제, 매장명 출력 위치를 상단 조명 아래로 조정 / 2026-08-25: 2줄이던 안내 문구를 한 줄로 축약 + 폰트 축소, 전체 콘텐츠 출력 위치를 조명 아래로 더 이동)
- 쿠폰이 있으면:
  - `직원에게 보여주세요`
  - `{shortCode}`
  - `사용기한 ~{YYYY.MM.DD}`
- 버튼:
  - `daangn_url` 있으면 `[당근에서 단골 추가하기]` → 새 탭 `{daangn_url}`
  - 없으면 `당근 단골 링크 준비중` (빈 링크 이동 없음)
  - `[확인했어요]` → 로그아웃 후 화면 1

---

## 참고 A. 손님 계산대 QR (경로: `/checkout/[storeId]`)

목록 15개 밖이지만, 계산대에서 쿠폰 처리가 끝나려면 이 화면이 이어진다.  
소스: `app/(customer)/checkout/[storeId]/CheckoutClient.tsx`

로딩: `확인 중...`

미로그인:

- `계산대에서 사용하려면 로그인이 필요해요`
- 버튼: `[카카오로 시작하기]` → `/api/auth/kakao?storeId={storeId}&next=checkout`

로그인 후:

- `{storeName || storeId}`
- `계산대 대기`
- QR 꺼짐: `{message}` 또는 `직원에게 쿠폰 코드를 보여주세요.`
- 아이템 없음: `사용할 쿠폰이나 리워드가 없어요`
- 아이템 카드:
  - `{display_code}` (대기번호, 예: A1)
  - `{label}`
  - `{short_code}` (있을 때)
  - `이 번호를 직원에게 말씀해주세요`

다음 화면으로: 직원이 `/staff`에서 해당 대기번호를 처리.

---

## 화면 11. 계산대 검증 (경로: `/staff`)

소스: `app/staff/StaffClient.tsx`  
미로그인 시 `/admin/login?redirect=/staff`.

공통 헤더:

- `계산대`
- `{storeId} · {role}`
- 버튼: `[로그아웃]` → `/admin/login`
- 탭: `[QR 대기]` 또는 `[QR 대기 ({n})]` / `[코드 입력]`

### 11-A. 조회 전 — QR 대기 탭

- QR 꺼짐: `QR 자동조회가 꺼져 있습니다. 코드 입력 탭을 사용하세요.`
- 대기 없음: `대기 중인 손님이 없습니다`
- 대기 카드:
  - `{display_code}`
  - `{label}` 또는 `{amount}원`
  - `쿠폰 · 단골 확인 대기` 또는 `쿠폰 · 할인 적용 대기`
  - `리워드 · 단골 확인 대기` 또는 `리워드 · 할인 적용 대기`

### 11-B. 조회 전 — 코드 입력 탭

- 버튼: `[쿠폰]` `[리워드]`
- input placeholder: `6자리 코드`
- 버튼: `[조회]`

### 11-C. 조회 후 — 단골 확인 전 (`pending_verify`)

- `{display_code}`
- `{label}` (예: `{amount}원 쿠폰`)
- `{amount}원` 또는 `{amount}P` · `{short_code}`
- `손님 폰의 당근 단골 화면을 확인한 뒤 아래를 눌러주세요. 확인 방법은 매장 재량입니다.`
- 버튼: `[확인함]` / `[미확인 처리]`

이미 사용: `이미 사용된 경품입니다`  
만료: `사용기간이 지난 경품입니다`  
이미 확인됨: `할인 적용 안내가 열려 있습니다`

### 11-D. 미확인 처리

- `미확인 사유`
- 버튼: `[앱없음]` `[거부]` `[기타]`
- `[취소]` `[미확인 확정]`

### 11-E. 확인함 이후 — 전체 화면 배너 (닫히지 않음)

- `{display_code}`
- `{amount}원` (쿠폰) 또는 `{label}` (리워드)
- `계산대에서 이 금액을 적용해주세요`
- 버튼: `[할인 적용 완료]` / 로딩 시 `[처리 중...]`

에러 예: `{data.error}` 또는 `처리에 실패했습니다` / `네트워크 오류가 발생했습니다` / `조회에 실패했습니다`

다음 화면으로: `[할인 적용 완료]` 성공 시 배너 닫힘, 대기열 갱신. 손님 여정 종료.

---

## 화면 12. 관리자 로그인 (경로: `/admin/login`)

소스: `app/admin/login/page.tsx`

- 상단:
  - 🥕
  - `관리자 로그인`
  - `단골마케팅 내부 관리자 전용`
- 본문 (폼):
  - 라벨 `이메일` / placeholder `admin@example.com`
  - 라벨 `비밀번호` / placeholder `••••••••`
- 버튼: `[로그인]` / `[로그인 중...]`
- 하단: `손님용 게임 화면과는 별개입니다`
- 에러 예: `{data.error}` 또는 `로그인에 실패했습니다` / `네트워크 오류가 발생했습니다`

다음 화면으로:

- `staff` → `/staff` (화면 11)
- 그 외 → `/admin/events` (화면 13)
- `?redirect=` 있으면 그 경로

---

## 화면 13. 이벤트 관리 (경로: `/admin/events`)

소스: `app/admin/(auth)/events/page.tsx` + `EventCard.tsx`  
공통 내비 (`AdminNav.tsx`): `🥕 단골마케팅` + 역할별 메뉴 + `{역할명}` + `{email}` + `[로그아웃]`

슈퍼관리자/에이전시 메뉴: `전체 대시보드` `업체 리스트` `이벤트 관리` `성과 리포트` `포인트 정책` `리워드 관리` `쿠폰 발급`  
광고주 메뉴: `이벤트 관리` `성과 리포트` `포인트 정책` `리워드 관리` `쿠폰 발급` `계산대 →`

페이지:

- 상단: `이벤트 관리`
- `매장: {filterStoreId}` (필터 있을 때)
- 버튼: `[+ 새 이벤트 등록]` → `/admin/events/new`

비어 있을 때:

- `등록된 이벤트가 없습니다`
- `우측 상단 버튼으로 첫 이벤트를 등록해보세요.`

카드가 있을 때 (각 이벤트):

- 상태 뱃지: `진행중` / `예정됨` / `일시중지` / `종료됨` / `초안`
- `{store_id}`
- `{event.name}`
- `{display_start_date} ~ {display_end_date}`
- 토글 (진행중/일시중지): title `클릭하면 일시중지` 또는 `클릭하면 진행 중으로 변경`
- 버튼: `[복사]` `[수정]` `[미리보기 →]`
  - 복사 → `/admin/events/new?copy=...`
  - 수정 → `/admin/events/{id}`
  - 미리보기 → `/play/{store_id}` (새 탭)

다음 화면으로: 위 링크.

---

## 화면 14. 포인트 정책 설정 (경로: `/admin/loyalty-settings`)

소스: `LoyaltySettingsClient.tsx` + `StoreSelector.tsx`

- 상단: `포인트 정책 설정`
- 매장 선택 (광고주는 숨김, 슈퍼/에이전시만):
  - `관리할 매장`
  - `불러오는 중...`
  - 셀렉트 첫 옵션: `-- 매장을 선택하세요 --`
  - 옵션: `{store_name} ({store_id})`
  - `매장 선택 후 이용 가능합니다`
- 매장 미선택: `위에서 매장을 선택하면 정책을 설정할 수 있습니다`
- 저장 메시지: `✅ 저장되었습니다` / `{error}` / `저장 실패` / `매장을 먼저 선택해주세요`

매장 선택 후 폼:

- `포인트 적립 사용`
- `끄면 게임 완료 시 포인트 적립을 하지 않습니다. 쿠폰 발급은 그대로입니다.`
- (온/오프 스위치, 라벨 텍스트 없음)
- `방문 1회당 적립 포인트`
- 단위: `P / 회`
- `게임 완료 시 꽝 포함 무조건 적립됩니다`
- `리워드 사용 가능 최소 잔액`
- 단위: `P 이상 보유 시 교환 가능`
- `현재 설정 기준: {visitsNeeded}번 방문하면 사용 가능해요`
- `포인트 유효기간`
- input placeholder: `미입력 시 무제한`
- 단위: `일`
- `평균 재방문 주기` `(세그먼트 분류 기준)`
- 단위: `일`
- `손님별 방문 이력이 3회 미만일 때 세그먼트(이탈 위험·휴면) 판정에 사용하는 기준값이에요.`
- `업종 특성에 맞게 조정하세요 (기본값 7일).`
- 버튼: `[저장]` / `[저장 중...]`

다음 화면으로: 저장 후 같은 화면.

---

## 화면 15. 업체 리스트 (경로: `/admin/companies`)

소스: `app/admin/(auth)/companies/page.tsx`  
권한: `super_admin` / `agency`만. 그 외는 `/admin/events`로 보냄.

- 상단:
  - `업체 리스트`
  - `계약 현황 및 담당자 관리`
- 버튼: `[+ 업체 등록]` → `/admin/companies/new`

비어 있을 때:

- `등록된 업체가 없습니다`
- `우측 상단 버튼으로 첫 업체를 등록해보세요.`

카드가 있을 때:

- `{store_name}` `{store_id}`
- 뱃지: `계약 만료` 또는 `D-{days} 만료임박`
- `계약: {contract_start_date} ~ {contract_end_date}`
- `담당자: {manager_name || '-'} · 계약자: {contractor_name || '-'}`
- `{ad_amount}원`
- `월 광고비`

카드 클릭 → `/admin/companies/{id}`

다음 화면으로: 업체 상세/등록.

---

## 손님 여정 한 줄 (이 문서 순서)

```
1 랜딩 → 2 게임 → 3 결과 잠금 → 4 카카오 →
  (이미 참여면 5에서 끝)
  6 결과(+7 포인트 한 줄) → 8 채널 건너뛰기 →
  (당첨이면 10 단골 안내) →
  참고 A 손님 계산대 QR → 11 직원 계산대 (확인함 → 할인 적용 완료)
```

9 내 포인트, 12~15 관리자 화면은 위 손님 본선과 별 경로다.
