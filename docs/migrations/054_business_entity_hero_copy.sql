-- ============================================================
-- Migration 054: 매장 홈페이지 히어로 카피 커스터마이징
-- 실행 위치: Supabase Dashboard > SQL Editor
--
-- 지금까지는 히어로 섹션에 "한 줄 소개"(description) 하나만 노출되고,
-- 게임 버튼 문구("게임하고 쿠폰받기 🎮")는 코드에 고정되어 있었다.
-- 매장마다 감성적인 헤드라인(줄바꿈 2줄)과 게임 버튼 문구를 다르게
-- 보여줄 수 있도록 두 필드를 추가한다. 둘 다 선택 입력이며, 비어있으면
-- 기존과 동일하게 동작한다(하위 호환).
-- ============================================================

ALTER TABLE business_entity
  ADD COLUMN IF NOT EXISTS tagline        text,  -- 히어로 메인 카피 (줄바꿈 유지, 예: "오늘 먹고 끝내지 말고,\n다음에도 생각나는 고깃집.")
  ADD COLUMN IF NOT EXISTS game_cta_label text;  -- 게임 버튼 문구 (예: "🎮 오늘의 고기 뽑기"), 비어있으면 기본 문구 사용
